export const loadProjects = async () => {
  return []; // later: load from IndexedDB or backend
};

export const createProject = async () => {
  // open modal or auto-create a new project ID
};

export const deleteProject = async (projectId: string) => {
  // delete from DB
};
